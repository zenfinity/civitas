# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**civitas** is a tool for finding and contacting government officials.

This repository is in its initial state — no source code has been added yet. The `.gitignore` is configured for a Node.js/TypeScript project and includes patterns for Next.js, Nuxt, SvelteKit, Vite, and other common JS frameworks, suggesting the project will be built in that ecosystem.

## Development Setup

Once source code is added, update this file with:
- How to install dependencies (e.g., `npm install` / `pnpm install`)
- How to run the development server
- How to build for production
- How to run tests and linting
- How to run a single test file

## Architecture

Once the codebase exists, document here:
- The chosen framework and why
- How data about government officials is sourced and structured
- How search/lookup is implemented
- How contact functionality works (email, form submission, etc.)
- Any external APIs or data providers used
